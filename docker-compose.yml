services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: bladevault:latest
    container_name: bladevault
    ports:
      - "5500:3000"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=3000
